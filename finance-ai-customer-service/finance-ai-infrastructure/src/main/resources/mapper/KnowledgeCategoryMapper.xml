<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.company.finance.infrastructure.mapper.KnowledgeCategoryMapper">

    <resultMap id="categoryResultMap" type="com.company.finance.domain.entity.KnowledgeCategory">
        <id property="categoryId" column="category_id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectAll" resultMap="categoryResultMap">
        SELECT category_id, code, name, sort_order, created_at, updated_at
        FROM t_knowledge_category
        ORDER BY sort_order, created_at
    </select>

    <select id="selectById" resultMap="categoryResultMap">
        SELECT category_id, code, name, sort_order, created_at, updated_at
        FROM t_knowledge_category
        WHERE category_id = #{categoryId}
    </select>

    <insert id="insert">
        INSERT INTO t_knowledge_category (category_id, code, name, sort_order, created_at, updated_at)
        VALUES (#{categoryId}, #{code}, #{name}, #{sortOrder}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE t_knowledge_category
        SET code = #{code}, name = #{name}, sort_order = #{sortOrder}, updated_at = NOW()
        WHERE category_id = #{categoryId}
    </update>

    <delete id="deleteById">
        DELETE FROM t_knowledge_category WHERE category_id = #{categoryId}
    </delete>

</mapper>
